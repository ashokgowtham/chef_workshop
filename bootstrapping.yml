1: add /etc/hosts entries or dns entries if needed:
  - node1, node2, chef-server
2: bootstrapping a new machine:
  - knife bootstrap NODE-HOST-NAME/IP --ssh-user SSH-USER-NAME --ssh-pass SSH-PASSWORD --sudo --node-name node2 --run-list 'recipe[simple-website]'
3: done:
  